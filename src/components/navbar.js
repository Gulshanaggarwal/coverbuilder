import { Box } from "@mui/system"

export default function Navbar() {
    return (
        <Box component="header" color="white" padding="1rem" sx={{ backgroundColor: 'primary.dark', gridColumnStart: 1, gridColumnEnd: 4 }}>Cover-Builder</Box>
    )
}